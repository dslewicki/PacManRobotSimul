#ifndef COORDINDEX
#define COORDINDEX
	int coordToIndex(int, int, int);//converts a given (rownum,colnum) to the appropiate index, with a given length/width
	int indexToRownum(int, int);//converts a given index(i) to the appropiate rownum, given length/width
	int indexToColnum(int, int);//converts a given index(i) to the appropiate colnum, given length/width
#endif
	